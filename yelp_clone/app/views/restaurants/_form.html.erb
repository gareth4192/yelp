<div class="form-group">
<%= form_for @restaurant, html: {multiple: true} do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
</div>
<div class="form-group">
  <%= f.file_field :image %>
  </div>
  <%= f.submit %>
<% end %>
